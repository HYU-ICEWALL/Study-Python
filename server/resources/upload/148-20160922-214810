

def isDivisor(num) :
	i=0;
	divisor = 0;
	for i in range(1,num+1):
		if(num%i == 0):
			divisor += 1;
	return(divisor == 2)



for i in range(100) :
	a = input()
	if(isDivisor(int(a))) :
		print ("1")
		continue
	print ("0")